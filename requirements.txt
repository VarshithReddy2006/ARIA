# LLM provider (DeepSeek via NVIDIA NIM — OpenAI-compatible REST)
httpx>=0.27.0

# Local BGE embeddings — no API calls, no quotas
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.4.0

# Data validation and serialization.
# Aligned with MCP SDK (>=2.8.0) and Google GenAI SDK (>=2.7.0).
pydantic>=2.8.0,<3.0.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP utilities
requests>=2.31.0

# Testing
pytest>=7.4.0

# FastAPI web framework + ASGI server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Graph analysis (architecture / reading-order / impact analysis)
networkx>=3.0

# Tree-sitter parsing (chunking pipeline)
tree-sitter>=0.21.0,<0.22.0
tree-sitter-python>=0.21.0,<0.22.0
tree-sitter-javascript>=0.21.0,<0.22.0
tree-sitter-typescript>=0.21.0,<0.22.0

# Centralised configuration & CLI
pydantic-settings>=2.0.0
typer>=0.9.0

# Google GenAI SDK
google-genai>=0.1.1

# Model Context Protocol (MCP) server SDK.
# Upper bound is deliberate: SDK 2.x removed mcp.server.fastmcp and the FastMCP
# class entirely (replaced by mcp.server.mcpserver), so 2.x cannot import this
# codebase's MCP layer. See docs/MCP_RELEASE_READINESS.md for the migration path.
mcp[cli]>=1.0.0,<2
