# Base URL of the FastAPI backend.
# In development (npm run dev), defaults to http://127.0.0.1:8001 if unset.
# In production / Docker, leave empty so requests use same-origin relative URLs (/api/v1/...).
PUBLIC_API_URL=

